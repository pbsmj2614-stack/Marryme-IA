# ── Dependências ──
node_modules/
frontend/node_modules/

# ── Variáveis de ambiente (nunca commitar) ──
.env
.env.local
frontend/.env.local
frontend/.env
backend/.env

# ── Build / Cache Next.js ──
frontend/.next/
frontend/tsconfig.tsbuildinfo

# ── Credenciais Google ──
backend/credenciais.json

# ── Outputs gerados pelo script Python ──
backend/outputs/

# ── Supabase local ──
supabase/.temp/

